[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate", "DATABASE_URL=\"postgresql://dummy:dummy@localhost:5432/dummy\" npm run build"]

[start]
cmd = "npm start"
