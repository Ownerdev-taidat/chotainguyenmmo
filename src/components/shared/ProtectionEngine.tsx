'use client';

/**
 * ProtectionEngine — LIGHT MODE (v2.0)
 * =====================================
 * Antigravity Stealth — KHÔNG block UX
 * Chỉ giữ security headers + httpOnly cookie (server-side)
 * Khách F12 thoải mái, trang sáng 100%
 */

export default function ProtectionEngine() {
    return null; // Light Protection Mode — no client-side blocking
}
