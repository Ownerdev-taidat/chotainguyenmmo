'use client';

/**
 * ContentProtection — DISABLED (Light Mode)
 * ==========================================
 * Không chặn right-click, copy, select, print, drag
 * Không watermark
 * Khách dùng trang bình thường 100%
 */

export default function ContentProtection(_props: any) {
    return null;
}
