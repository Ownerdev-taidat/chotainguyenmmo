'use client';

/**
 * AntiDebugOverlay — DISABLED (Light Mode)
 * =========================================
 * Không hiện overlay, không blur, không cảnh báo
 */

export default function AntiDebugOverlay(_props: any) {
    return null;
}
